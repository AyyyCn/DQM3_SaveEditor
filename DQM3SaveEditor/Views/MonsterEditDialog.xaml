<Window x:Class="DQM3SaveEditor.Views.MonsterEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DQM3SaveEditor.Views"
        mc:Ignorable="d"
        Title="Edit Monster" Height="450" Width="400"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Name:"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Kind:"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Kind, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Level:"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Level, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Row="3" Grid.Column="0" Content="Size:"/>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Size, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Row="4" Grid.Column="0" Content="Unspent Points:"/>
        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding UnspentPoints, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Row="5" Grid.Column="0" Content="HP:"/>
        <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding HP, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Row="6" Grid.Column="0" Content="MP:"/>
        <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding MP, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Row="7" Grid.Column="0" Content="ATK:"/>
        <TextBox Grid.Row="7" Grid.Column="1" Text="{Binding ATK, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Row="8" Grid.Column="0" Content="DEF:"/>
        <TextBox Grid.Row="8" Grid.Column="1" Text="{Binding DEF, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Row="9" Grid.Column="0" Content="AGI:"/>
        <TextBox Grid.Row="9" Grid.Column="1" Text="{Binding AGI, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Row="10" Grid.Column="0" Content="WIS:"/>
        <TextBox Grid.Row="10" Grid.Column="1" Text="{Binding WIS, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Row="11" Grid.Column="0" Content="Skills:"/>
        <DataGrid Grid.Row="11" Grid.Column="1" Grid.RowSpan="2"
                  ItemsSource="{Binding Skills}"
                  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Points" Binding="{Binding AllocatedPoints}"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="13" Grid.Column="0" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="75" Margin="0,0,10,0" Click="OkButton_Click"/>
            <Button Content="Cancel" Width="75" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window> 